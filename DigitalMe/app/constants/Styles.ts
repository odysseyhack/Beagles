import { StyleSheet } from "react-native";

export const textStyles: any = StyleSheet.create({
  headerText: {
    fontSize: 24,
    lineHeight: 44,
    paddingBottom: 10,
    textAlign: "center",
    color: "rgba(96,100,109, 1)"
  },
  listItemText: {
    padding: 5,
    fontSize: 18,
    color: "rgba(0,0,0,0.4)"
  }
});
